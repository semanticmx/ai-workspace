# AI Workspace Environment Variables
# Copy this file to .env and fill in your actual values

# GitHub Integration
GITHUB_TOKEN=your-github-personal-access-token

# Brave Search API (for web search MCP server)
BRAVE_API_KEY=your-brave-api-key

# OpenAI API (if using OpenAI services)
OPENAI_API_KEY=your-openai-api-key

# Anthropic API (if using Claude API directly)
ANTHROPIC_API_KEY=your-anthropic-api-key

# AWS Credentials (if using AWS MCP servers)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1

# Database URLs (for project-specific databases)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Custom MCP Server Ports (optional)
MCP_SERVER_PORT=3000
MCP_DEBUG_LEVEL=info

# Project Paths (optional overrides)
# AI_WORKSPACE=$HOME/ai-workspace
# GITHUB_DIR=$HOME/GitHub